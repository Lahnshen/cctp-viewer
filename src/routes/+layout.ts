// Fully static: the transfer index is a build-time artifact and every lookup
// after that runs in the browser against Circle and Injective directly.
export const prerender = true;
export const ssr = false;
